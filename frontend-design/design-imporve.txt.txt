Refine the GitSyntropy color system for better accessibility. Define a CSS variable scale for the primary hue (270°). Set --primary-solid to #7f00ff for buttons. Create --primary-text at #c480ff for all headings and accented links to ensure a 4.5:1 contrast ratio against the #121212 background. Apply a soft glow 0 0 10px rgba(196, 128, 255, 0.3) to headers. Update secondary buttons to use --primary-text for the label and a thin border of 1px solid rgba(127, 0, 255, 0.4). Maintain the Bento Grid and glassmorphic layout as previously defined.